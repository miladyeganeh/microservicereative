apply plugin: "war"

bootWar {
    mainClassName = "com.dv.reviewer.MicroservicereativeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
